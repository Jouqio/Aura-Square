// AuthProvider.tsx — Phase 6 stub (auth dihapus)
import React from 'react';
interface Props { children: React.ReactNode; }
export function AuthProvider({ children }: Props): React.JSX.Element {
  return <>{children}</>;
}
